<!DOCTYPE html>
<html lang="ni">
<head>
	<title> {{pageTitle}} </title>
	<meta charset="utf-8" />
	<link rel="stylesheet" href="{{ app.request.baseUrl }}/assets/css/bootstrap.css"/>
	<link rel="stylesheet" href="{{ app.request.baseUrl }}/assets/css/start.css" />
	<link rel="stylesheet" href="{{ app.request.baseUrl }}/assets/css/header.css" />
</head>
<body>
	{% if page is defined and page != 'noheader' %}
	<header class="header-search" id="header">

		<div class="header-limiter">

			<h1><a href="{{ path('login') }}"><img src="{{ app.request.baseUrl }}/assets/img/logo.png" alt=""></a></h1>

			<nav>
			{% if page is defined %}
				{% if page == 'home' %}
				<a href="{{ path('voluntarios.overview') }}">Voluntarios</a>
				<a href="{{ path('empleados.overview') }}">Personal</a>
				<a href="{{ path('logger.log') }}">Log</a>
				{% elseif page == 'voluntarios' %}
				<a href="{{ path('voluntarios.overview') }}"class="selected">Voluntarios</a>
				<a href="{{ path('empleados.overview') }}">Personal</a>
				<a href="{{ path('logger.log') }}">Log</a>
				{% elseif page == 'empleados' %}
				<a href="{{ path('voluntarios.overview') }}">Voluntarios</a>
				<a href="{{ path('empleados.overview') }}" class="selected">Personal</a>
				<a href="{{ path('logger.log') }}">Log</a>

				{% endif %}
				{% else %}
				<a href="{{ path('voluntarios.overview') }}">Voluntarios</a>
				<a href="{{ path('empleados.overview') }}">Personal</a>
				<a href="{{ path('logger.log') }}">Log</a>
				{% endif %}
			</nav>
			{% if session is defined %}
			{% if session != null %}
			
			<div class="loginNavigation">
				<a href="{{ path('logout') }}" class="btn btn-primary-outline">Cerrar sesión</a>
				
			</div>
			{% endif %}
			{% endif %}
		</div>
	</header>
	{% endif %}
	<div class="container" id="container">
		{% block pageContent %}
		Hello!
		{% endblock %}
	</div>
</body>
</html>